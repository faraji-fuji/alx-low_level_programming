#include <stdio.h>
#include <string.h>

/**
 * main - writes Programming is like bulding a multilingual puzzle
 *
 * Return:zero
 */
int main(void)
{
	char str1[52];

	strcpy(str1, "\"Programming is like bulding a multilingual puzzle\n");
	puts(str1);
	return (0);
}
